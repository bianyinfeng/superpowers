export interface TutorialStep {
  id: string;
  stage: number;
  title: string;
  content: string;
  module: 'vectordb' | 'features' | 'training' | 'agent' | 'comparison' | 'home';
}

export const tutorials: TutorialStep[] = [
  {
    id: 'home',
    stage: 0,
    title: '🏠 首页',
    module: 'home',
    content: `
# 🚀 从编程小白到智能体训练师

欢迎来到新一代程序员教程！本教程将带你从零开始，逐步掌握大模型训练与部署的核心概念。

## 教程特色

- **🌐 纯浏览器运行**：无需 GPU，无需安装任何软件
- **🎮 交互式学习**：每个概念都配有可操作的仿真模块
- **📈 渐进式难度**：从基础概念到高级应用，循序渐进
- **🇨🇳 全中文界面**：降低语言门槛

## 学习路径

1. **向量数据库** → 理解数据如何存储和检索
2. **特征提取** → 文本如何变成机器能理解的数字
3. **模型训练** → 神经网络如何学习
4. **Agent 沙箱** → 智能体如何工作
5. **模型对比** → 如何评估和选择模型

点击左侧导航栏开始学习！
    `.trim(),
  },
  {
    id: 'vectordb',
    stage: 2,
    title: '🗄️ 向量数据库',
    module: 'vectordb',
    content: `
# 向量数据库

## 什么是向量数据库？

向量数据库是一种专门用来存储和检索**向量**（一组数字）的数据库。传统数据库通过关键字搜索，而向量数据库通过**相似度**搜索。

## 为什么需要它？

在 AI 领域，文本、图片等数据会被转换为高维向量。向量数据库让我们能够：
- 快速找到语义相似的文本
- 实现 RAG（检索增强生成）
- 构建推荐系统

## 动手试试

使用右侧的仿真器：
1. **加载示例数据** — 插入几条中文文本
2. **尝试搜索** — 输入一个查询，观察相似度排序
3. **观察可视化** — 看看向量在 2D 空间中的分布
    `.trim(),
  },
  {
    id: 'features',
    stage: 2,
    title: '🔬 特征提取',
    module: 'features',
    content: `
# 特征提取

## 什么是特征提取？

特征提取是将原始数据（如文本）转换为机器可以处理的数值表示的过程。

## 常见方法

### 词袋模型 (Bag of Words)
统计每个词出现的次数，形成一个向量。

### TF-IDF
- **TF (词频)**：一个词在文档中出现的频率
- **IDF (逆文档频率)**：衡量一个词的"罕见程度"
- TF-IDF = TF × IDF，值越大说明这个词对这篇文档越重要

### 嵌入向量 (Embeddings)
使用预训练模型将文本映射到高维空间，语义相似的文本在空间中距离更近。

## 动手试试

1. 添加几篇不同主题的文档
2. 点击文档查看分词结果和嵌入向量
3. 计算 TF-IDF，观察热力图中的特征重要性
    `.trim(),
  },
  {
    id: 'training',
    stage: 4,
    title: '🧠 模型训练',
    module: 'training',
    content: `
# 模型训练

## 神经网络是什么？

神经网络是由很多"神经元"连接而成的网络，能从数据中学习规律。

## 训练过程

1. **前向传播**：数据通过网络，计算预测值
2. **计算损失**：预测值与真实值的差距（损失函数）
3. **反向传播**：计算每个参数对损失的贡献
4. **更新参数**：按梯度方向调整参数（优化器）
5. **重复**：直到损失足够小

## 关键超参数

- **学习率**：参数更新的步长。太大会震荡，太小收敛慢
- **批次大小**：每次用多少数据更新参数
- **训练轮次**：遍历完整数据集的次数
- **隐藏层大小**：网络的"容量"

## 动手试试

1. 使用默认配置训练一次，观察曲线
2. 把学习率调到 0.001 再训练，对比收敛速度
3. 把学习率调到 0.9 看看会发生什么
4. 增大隐藏层到 64，观察过拟合现象
    `.trim(),
  },
  {
    id: 'agent',
    stage: 5,
    title: '🤖 Agent 沙箱',
    module: 'agent',
    content: `
# 智能体 (Agent)

## 什么是 AI Agent？

Agent 是能够自主完成任务的 AI 系统。它不只是"回答问题"，而是：
- **感知**环境和用户输入
- **思考**下一步该做什么
- **行动**：调用工具、生成回答
- **反思**：评估结果，决定是否需要更多操作

## Agent 的核心组件

1. **大语言模型 (LLM)**：Agent 的"大脑"
2. **工具 (Tools)**：Agent 可以调用的外部功能
3. **提示词 (Prompt)**：指导 Agent 行为的指令
4. **记忆 (Memory)**：Agent 的对话历史和知识

## RAG Pipeline

RAG = 检索增强生成：
1. 用户提问 → 2. 搜索知识库 → 3. 将相关信息注入 Prompt → 4. LLM 生成回答

## 动手试试

1. 和模拟 Agent 对话，观察它的工作流程
2. 注意当触发工具调用时的处理过程
3. 查看工具列表，理解每个工具的功能
    `.trim(),
  },
  {
    id: 'comparison',
    stage: 6,
    title: '📊 模型对比',
    module: 'comparison',
    content: `
# 模型对比与评估

## 为什么要对比模型？

没有"最好"的模型，只有"最合适"的模型。通过对比实验，我们可以：
- 找到最优的超参数组合
- 理解不同配置的影响
- 在准确率、速度、复杂度之间做权衡

## 评估指标

- **准确率 (Accuracy)**：正确预测的比例
- **损失 (Loss)**：预测值与真实值的差距
- **过拟合程度**：训练准确率与测试准确率的差距
- **训练时间**：达到收敛需要的时间

## A/B 测试方法

1. **控制变量**：每次只改变一个超参数
2. **使用相同数据集**：保证公平比较
3. **多次实验**：消除随机性的影响

## 动手试试

1. 点击"开始对比实验"
2. 观察四个不同配置的模型的表现差异
3. 思考：为什么某些配置效果更好？
4. 看看雷达图中各模型的综合表现
    `.trim(),
  },
];
